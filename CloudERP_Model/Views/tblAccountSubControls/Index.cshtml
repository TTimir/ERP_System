@model IEnumerable<DatabaseAccess.tblAccountSubControl>

@{
    string controllerName = "tblAccountSubControls";
    ViewBag.ControllerName = controllerName;
    ViewBag.MainTitle = "Account's";
    ViewBag.Title = "All Account Sub Controls";
}


<a href="@Url.Action("Create")" class="btn btn-primary m-b-20">
    <i class="icon-plus mr-1"></i> Add Sub Account Control
</a>

<div class="row clearfix">
    <div class="col-lg-12">
        <div class="card">
            <div class="body">
                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-hover dataTable js-exportable">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>
                                    Head Account
                                </th>
                                <th>
                                    Sub Control Account
                                </th>
                                <th>
                                    Control Account
                                </th>
                                <th>
                                    Add (by)
                                </th>
                                <th>Action's</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.Count(); i++)
                            {
                                var item = Model.ElementAt(i);
                                <tr>
                                    <td>@(i + 1)</td> <!-- Serial Number Column -->
                                    <td>
                                        @Html.DisplayFor(modelItem => item.tblAccountHead.AccountHeadName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.AccountSubControlName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.tblAccountControl.AccountControlName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.tblUser.FullName)
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Edit", new { id = item.AccountSubControlID })" class="btn btn-sm btn-icon btn-pure btn-default on-default m-r-5 button-edit" data-toggle="tooltip" data-original-title="Update">
                                            <i class="icon-pencil"></i>
                                        </a>
                                        @*<a href="@Url.Action("Details", new { id = item.AccountSubControlID })" class="btn btn-sm btn-icon btn-pure btn-default on-default m-r-5 button-info" data-toggle="tooltip" data-original-title="Details">
                                                <i class="icon-info"></i>
                                            </a>*@
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Sr.No</th>
                                <th>
                                    Account Head
                                </th>
                                <th>
                                    Account Controler
                                </th>
                                <th>
                                    User
                                </th>
                                <th>Action's</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
